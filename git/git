sudo apt-get install git
git config --global user.name "Your Name"
git config --global user.email "email@example.com"


git init
git add <file> (-u -A)
git commit -m <message>

git status

git diff <filename>
git diff HEAD -- <filename>

git log
git log --pretty=oneline

git reset --hard HEAD^(HEAD~<num>)
git reset --hard <commit ID>

git reflog

git checkout -- <filename>(撤销工作区）
git reset HEAD <filename>(撤销暂存区）

git rm <filename>
or rm <filename> then git add <filename>

ssh-keygen -t rsa -C "youremail@example.com"
git remote add origin git@github.com:michaelliao/learngit.git
git push -u origin master(First time)
git push origin master

git clone git@github.com:universalhenry/XXX.git

git branch
git branch <name>
git checkout <name>
git checkout -b <name>
git merge <name>
git branch -d <name>
git branch -D <name>(forced)

git log --graph

git merge --no-ff -m "commit" <name>(keep history)

git stash
git stash list
{
git stash apply
git stash drop
}==git stash pop

git remote
git remote -v
git push origin branch-name
git checkout -b branch-name origin/branch-name
git pull
git branch --set-upstream-to=origin/dev dev

git rebase

git tag <tagname>
git tag -a <tagname> -m "blablabla..."
git tag
git push origin <tagname>
git push origin --tags
git tag -d <tagname>
git push origin :refs/tags/<tagname>

https://gitee.com/
git remote rm <origin-name>
git remote add <origin-name> git@gitee.com:liaoxuefeng/learngit.git

.gitignore

cat .git/config
cat .gitconfig
git config [--global] alias.st status


